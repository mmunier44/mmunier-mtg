{{#each cards as |card|}}
  <!-- {{log book}}
  {{book section}} -->
  <!-- {{> partial}}
  Don't uncomment yet -->

  <!-- <div>
    <h1 data-id="{{book.id}}"> <small>Title: {{book.title}}</small></h1>
    {{#if book.author}}
      {{book.author}}
    {{else}}
      No author provided.
    {{/if}}
  </div> -->

  <div>
    <h1 data-id="{{card.id}}"> <small>Title: {{card.name}}</small></h1>
    <section data-id="{{card.id}}">
      <p>{{#card.id}}</p>
      <p>{{#card.name}}</p>
      <p>{{#card.description}}</p>
      <button type="submit" class="btn btn-default">Remove</button>
    </section>
  </div>

{{/each}}
