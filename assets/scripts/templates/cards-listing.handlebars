
{{#each cards as |card|}}
  {{log card}}
  <!-- {{> partial}}
  Don't uncomment yet -->

  <div>
    <h1 data-id="{{card.id}}"> <small>Name: {{card.name}}</small></h1>
    <section data-id="{{card.id}}">
      <p>{{#if card.name}}
          By: {{card.name}}
        {{else}}
          No name provided.
        {{/if}}
      </p>
      <button type="submit" class="btn btn-default">Remove</button>
    </section>
  </div>

{{/each}}
